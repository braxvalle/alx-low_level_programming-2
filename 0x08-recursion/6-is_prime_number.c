#include "main.h"
/**
 * is_prime_number - finds a prime number
 * @n: an integer value
 * Return: returns a prime number
 */
int is_prime_number(int n)
{
	if (n / n == 0 && n / 1 == n)
		return (1);

		return (0);
}
