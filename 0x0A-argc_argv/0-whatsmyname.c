#include <stdio.h>
/**
 * main - entry point of program
 * @argc: integer
 * @argv: character string
 * Return: always 0 (success)
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
