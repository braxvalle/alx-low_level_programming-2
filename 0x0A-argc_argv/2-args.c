#include <stdio.h>
/**
 * main - entry point
 * @argc: argument count
 * @argv: argument strings
 * Return: 0
 */
int main(int argc, char **argv)
{
	argv = argv;
	printf("argc[%d]: %s\n", argc);
	return (0);
}
