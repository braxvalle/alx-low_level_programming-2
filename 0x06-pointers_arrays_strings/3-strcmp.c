#include "main.h"
/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 */
int _strcmp(char *s1, char *s2)
{
	int i;
	int j;
	
	if (i > j)
	{
		dest = dest[i];
	}
	else if (i < j)
	{
		dest = dest[j];
	}
	else 
		dest = 0;
	return (dest);
}
