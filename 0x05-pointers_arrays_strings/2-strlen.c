#include "main.h"
/**
 * int _strlen(char *s) - returns the lenth of a string
 * @s; represents the string
 * Return; always 0
 */
int _strlen(char *s)
{
	int len;

	len = _strlen(*s);
	return (0);
}

